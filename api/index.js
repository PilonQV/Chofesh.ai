// Vercel Serverless Function - Proxy to Express server
// This is a placeholder - Vercel needs a different architecture for full-stack apps

export default function handler(req, res) {
  res.status(200).json({ 
    message: "API endpoint",
    note: "Full Express server requires different deployment approach"
  });
}
